# Infrastructure

This directory is reserved for public, secret-free infrastructure code.

The foundation pull request intentionally does not add Docker Compose, service connections, routing, TLS, deployment, database, Redis, Socket.IO, or email-provider configuration. Those changes require their own reviewed scope and verification.
