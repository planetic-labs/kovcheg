services:
  api-1:
    build:
      target: realtime-test-runtime
    environment:
      KOVCHEG_IDENTITY_STUB_ENABLED: 'true'
      NODE_ENV: test

  api-2:
    build:
      target: realtime-test-runtime
    environment:
      KOVCHEG_IDENTITY_STUB_ENABLED: 'true'
      NODE_ENV: test
