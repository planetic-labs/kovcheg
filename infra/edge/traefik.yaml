api:
  dashboard: false

entryPoints:
  internal:
    address: ':8081'
  local:
    address: ':8080'

log:
  format: json
  level: WARN

ping:
  entryPoint: local

providers:
  file:
    filename: /etc/traefik/routes.yaml
    watch: false
